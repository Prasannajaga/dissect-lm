[project]
name = "dissectlm-python"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "transformers>=4.40",
  "torch>=2.2",
  "tensorflow>=2.15",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
